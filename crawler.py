"""
the crawler’s job is to find all internal pages
on a target website so the scanner can test them for vulnerabilities.
It extracts
✅ All internal links (same domain)

✅ Forms on each page (for input injection)
git
✅ GET/POST parameters (e.g. ?id=1, ?search=abc)
"""

